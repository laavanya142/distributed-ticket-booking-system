spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/event_db
  data:
    redis:
      host: redis
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
logging:
  level:
    root: INFO
    com.ticketbooking.event: INFO

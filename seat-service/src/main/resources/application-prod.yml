spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/seat_db
  data:
    redis:
      host: redis
  kafka:
    bootstrap-servers: kafka:29092
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
logging:
  level:
    root: INFO
    com.ticketbooking.seat: INFO
